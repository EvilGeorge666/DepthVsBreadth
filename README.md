# BFS + DFS Pathfinding (Python Console)

## Run
```bash
python pathfinding.py
```

The script runs BFS and DFS on two built-in maps and prints:
- whether a path was found
- path length
- visited node count
- rendered maps with visited/path overlays
